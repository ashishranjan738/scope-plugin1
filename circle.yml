general:
  branches:
    ignore:
      - gh-pages

machine:
  services:
    - docker
  environment:
    GOPATH: /home/ubuntu
    SRCDIR: /home/ubuntu/scope-plugin1
    PATH: $PATH:$HOME/.local/bin

dependencies:
  cache_directories:
    - "~/docker"
  override:
    - echo "no dependencies"

test:
  override:
    - cd $SRCDIR/iops/vendor/golang.org/x
    - rm -rf test net
    - git clone https://github.com/golang/net
    - git clone https://github.com/golang/text
    - cd
    - cd $SRCDIR/iops  && make .iops.uptodate && docker tag weaveworksplugins/scope-iops pradeepkumar95/iops:$(./tools/image-tag):

deployment:
  stag:
    branch: master
    commands:
      - docker login -e $DOCKER_EMAIL -u $DOCKER_USER -p $DOCKER_PASS
      - docker push pradeepkumar95/iops:$(./tools/image-tag)
